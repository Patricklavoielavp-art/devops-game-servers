#!/bin/bash

BASE_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"

source "$BASE_DIR/colors.sh"
source "$BASE_DIR/logging.sh"
source "$BASE_DIR/system.sh"
source "$BASE_DIR/network.sh"
source "$BASE_DIR/backup.sh"
source "$BASE_DIR/steamcmd.sh"